const { Products, Categories, Branches } = require('../db');
const { API_Key } = process.env;
const api_products = require('../data/data.json')

const getBranches = async () => {

}

module.exports = { getBranches }